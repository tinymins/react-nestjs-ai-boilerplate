# ======================================
# Server 环境变量 (本地开发)
# ======================================

# 数据库连接 URL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/apps_db

# 单一空间模式覆盖 (可选)
# 设置后将覆盖数据库中的设置，并在用户界面完全隐藏此选项
# 可选值: true | false (不设置则使用数据库值)
# SINGLE_WORKSPACE_MODE_OVERRIDE=true
